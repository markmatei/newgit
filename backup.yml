---
-name:general config
hosts:TestR1
gather_facts:no
connection:local

tasks:
 -name:show run
  ios_command:
      commands:
         -show run
      hosts:"{{ansible_host}}"
      username:george
      password:passwd
      register:config

-name:save output to /etc/ansible/Network-Programmability/Backups
copy:
  content:"{{config.standout[0]}}"
  dest:"/etc/ansible/Network-Programmability/Backups/running_{{invertory_hostname}}.txt"
